/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['nexus.leagueoflegends.com','i.imgur.com'],
  },
  }
  
module.exports = nextConfig
